Uses bootstrap 
Styled in a meaningful way
Takes in user input
Saves values into some type of variable via an event listener
Has another eventListener that will randomly display one value
That value will be displayed on the DOM  
